import { Layout } from '../../Components/Layout';
import style from './style.module.css';

const Homepage = () => {
  return (
    <Layout className={style.layout}>
      Homepage</Layout>
  )
}

export { Homepage }
